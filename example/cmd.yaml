# to be used with slider_publisher
cmd:
    type: std_msgs/Float64MultiArray
    v:
        min: -1
        max: 1
        to: data[0]
    beta_dot:
        min: -1
        max: 1
        to: data[1]

